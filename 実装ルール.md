# 実装ルール（3D連結マージ デモ）

このプロジェクトで実装を続ける際に守るルールを定義する。`仕様書.md`（何を作るか）・`設計書.md`（どう作るか）に対して、本書は「実装する際にどう書くか」のコーディング規約にあたる。

## 1. 技術方針（前提）

- npm・ビルドツールは使用しない。すべてのライブラリはCDN経由で`<script type="importmap">`から読み込む
- 物理エンジン（Cannon-es/Rapier等）は使用しない。合体・接続判定はグリッド座標上のロジックのみで行い、見た目の演出はTweenで表現する
- 上記2点を変更する場合は、事前に方針転換として相談する（ストレージ・実装コストとのトレードオフを検討した上での決定のため）

## 2. ディレクトリ構成とファイル配置ルール

```
src/
├── main.js      # 画面遷移・全体の配線のみ。ゲームロジックやThree.jsの詳細はここに書かない
├── game/        # Three.js・DOMに依存しない純粋なロジック（Node上で単体テスト可能な状態を保つ）
├── scene/       # Three.jsに依存する描画・アニメーション
├── input/       # 入力デバイスの状態管理（キー・カメラの意味づけはgame/側が持つ）
└── ui/          # DOM操作（画面の表示/非表示、テキスト更新、ボタンのイベント登録）
```

- **`game/` にThree.jsやDOMのAPIを持ち込まない。** ロジックのバグは目視確認できないことが多いため、Node上でimportして直接テストできる状態を維持する（実際に`GridModel`/`MergeResolver`/`ScoreManager`/`SpawnManager`はこの方針で単体テスト済み）
- 新しい画面を追加する場合は `ui/` に1クラス1ファイルで追加し、`index.html` に対応する `<div id="screen-xxx" class="screen hidden">` を用意する
- 新しい調整可能な数値（ゲームバランスに関わる値）は `game/Config.js` に追加する。ジオメトリのサイズや色のHEX値など、バランスに関係しない見た目専用の値は使用箇所の近く（`ObjectFactory.js`等）に置いてよい

## 3. 命名規則

- クラス・ファイル名: `PascalCase`（例: `GridModel.js` に `GridModel` クラス）。ファイル名とクラス名を一致させる
- メソッド・変数名: `camelCase`
- クラス内部だけで使う補助メソッドには `_` プレフィックスを付ける（例: `_updateCursorVisual`, `_resolveScreenDirection`）。外部から呼ばれる公開メソッドにはプレフィックスを付けない
- 色は英語の小文字文字列で統一する: `"white" | "yellow" | "red" | "blue" | "green"`（`Config.colors` の順序がそのまま段階順になる）
- 入力の方向は生の座標差分(`dx, dz`)ではなく、意味のある文字列 `"up" | "down" | "left" | "right"` で表現し、ワールド座標への変換は `GameController._resolveScreenDirection` に一本化する。dx/dzへの変換ロジックを複数箇所に重複させない
- 盤面上のオブジェクトは「エンティティ」と呼ぶ（`entityId`, `entity.cells`）。1マスのみを指す生の`{x,z,y}`セルと、複数マスをまとめた「エンティティ」を用語として混同しない

## 4. コーディングスタイル

- ES Modulesのみ使用。`import`/`export` はすべて相対パスで記述する
- 非同期処理（アニメーション待ち等）は `async`/`await` を使う。コールバックのネストは作らない
- アニメーションは `Promise` を返すヘルパー（`animateDropAsync`, `animateScaleAsync` 等）を用意し、呼び出し側で `await` する
- クラスのコンストラクタで完結する初期化（DOM要素の取得、イベントリスナー登録）は、コンストラクタ内にまとめて書く

## 5. コメント方針

- 基本はコメントを書かない。処理内容は変数名・関数名で表現する
- コメントを書くのは「なぜそうしているか」が非自明な場合のみ（例: なぜカメラ相対変換が必要か、なぜグリッド方式で物理エンジンを使わないか）
- 「何をしているか」を説明するコメント、実装の経緯・修正理由に言及するコメント（例:「〇〇の要望対応」）は書かない。経緯はコミットメッセージ側の役割とする

## 6. UI実装パターン

- 画面クラス（`TitleScreen`, `RuleScreen`, `SettingsScreen`, `HUD`, `PauseScreen`, `GameOverScreen`）は共通のパターンに従う
  - コンストラクタで `document.getElementById` によりDOM要素を取得する
  - コンストラクタで自身のボタンに `addEventListener` を登録し、実際の処理は呼び出し元から渡されたコールバック（`onXxx`）に委譲する
  - `show()` / `hide()` で `hidden` クラスの付け外しのみを行う
  - 表示内容の更新は `setScore()` のような専用メソッドで行う
- 画面間の配線（どのボタンでどの画面に遷移するか、`GameController` のどのメソッドを呼ぶか）は **`main.js` にすべて集約**する。UIクラス同士が直接依存しないようにする

## 7. ロジックとレンダリングの分離

- ゲームルールの変更（合体条件、スコア計算式、出現率等）は `game/` 内のロジックのみを直接編集すれば反映されるようにする。描画側（`scene/`, `ui/`）を触らずに済む設計を維持する
- 逆に、見た目の演出（形状・色・カメラ・アニメーション速度）の調整は `scene/` 内で完結させ、`game/` のロジックに手を入れない

## 8. Git運用

現時点でこのディレクトリはGitリポジトリとして初期化されていない。バージョン管理を始める場合は改めて相談すること（`git init` は破壊的な操作ではないが、いつ・どの粒度でコミットを切るかは方針を決めてから開始する）。
