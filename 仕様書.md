# 仕様書（3D連結マージゲーム デモ）

## 1. 概要

ぷよぷよ／1024（同色連結マージ）／スイカゲーム（合体演出）の要素を組み合わせた、3Dグリッド積み上げ型のパズルゲーム。

- 盤面サイズ（3×3〜6×6、初期値6×6）の床グリッド上に、プレイヤーが好きな位置を選んでオブジェクトを落とし、縦方向に積み上げていく
- **色の段階が上がるほど、オブジェクトは複数マス・複数段にまたがる立体形状（3Dミノ）になる**（白1マス→黄2マス→赤3マス→青4マス→緑5マス→紫6マス→橙7マス→黒8マス）。プレイヤーは回転操作でオブジェクトの向きを変えてから配置する
- カラー段階数（4〜8段階、初期値5段階）を設定で変更でき、難易度調整の主軸は**盤面サイズを小さくすること**（より直接的に難易度が上がる）。カラー段階数を増やすと、より大きく複雑な形状の段階が追加され、合体の連鎖に深みが出る（間接的な難易度上昇・長続き効果）
- 3Dミノは地形の凹凸に応じて、セルごとに異なる高さで着地しうる（4.2参照）。オブジェクト自体の見た目のサイズはマスの間隔より小さいため、同一エンティティ内で隣接するセル同士は（高さが同じでも）隙間ができる。視覚的な一体感を出すため、隣接するセル同士は常に「紐（ボンド）」で接続表示する（8節参照）
- 同色のオブジェクトが4つ以上、上下前後左右（6方向・斜めなし）に連結すると自動で合体し、1段階上の色のオブジェクト1個に変化する
- 色は設定した段階数ぶん（白→黄→赤→青→緑→紫→橙→黒の先頭から使用）。最後の段階が4つ以上連結すると消滅し、特大ボーナススコアが入る
- クリア（勝利）の概念はなく、いずれかの列が高さ上限に達して置き場所がなくなったらゲームオーバーとなるエンドレス型
- 初回実装はブラウザ（Three.js）で行い、将来的にUnityへ移植する前提のデモ

## 2. 盤面仕様

| 項目 | 値 | 備考 |
|---|---|---|
| 床グリッドサイズ | 3×3〜6×6（設定で変更可、初期値6×6） | 各マスが1つの「列（コラム）」。変更は次のゲーム開始時から反映 |
| 列の高さ上限 | 8段 | 初期値。後で調整可能な定数として持つ |
| 合体判定方向 | 6方向（上/下/前/後/左/右の面隣接のみ） | 斜め（辺・角）は含まない |
| 合体判定単位 | 同色4個以上の連結グループ（Flood Fill） | グループ全体が同時に消費される（マス数ではなく「個数」で判定） |
| オブジェクトの設置条件 | 占有する全セルが、地形かオブジェクト自身の別セルで支えられていること（浮き禁止） | 凸凹な地形にも着地できるが、隣接セルペアごとの高低差には上限がある |

複数マスを占有するオブジェクト（黄以降）は、地形の凹凸に合わせてセルごとに異なる高さで着地できる（3Dミノ）。ただし隣接する列同士の高低差には上限があり、それを超える配置はできない。この高低差は各列の一番下（縦積みの底）同士ではなく、**縦積みで隣の列に一番近づいているセル同士の隙間**で判定する（例: 縦2段積みの上段が隣の列と1段差まで近づいていれば、下段が2段差離れていても配置できる）。詳細は「4. 合体ルール」を参照。

## 3. 操作方法

- **カーソル移動＋確定ボタン**: 十字キー等で盤面グリッド上のカーソル（配置予定位置）を前後左右に移動し、決定キーで落とす
- **回転ボタン**: 縦回転・横回転の2つの独立したボタンを用意し、押すたびにそれぞれの軸で90度ずつ回転する。2軸の組み合わせで立体形状の全ての向き（24パターン）に到達できるため、3軸目（ロール相当）のボタンは設けない。白（1マス）のときは操作しても意味がないため、ボタンを無効化表示にする
- 配置予定のマスは**半透明グリッドでハイライト表示**することに加え、**現在の形状・向き・地形を反映した半透明の3Dゴースト**（実際に着地した際の形をそのまま縮小せず表示するプレビュー）を併せて表示する。配置不可（高低差の上限超過・支えなしなど）の場合は、ゴースト自体を赤みがかった無効表示にする（確定操作は無視される）
- 落下自体はリアルタイムの時間制限なし。プレイヤーが確定するまで何度でも移動・熟考できる
- **カメラ操作**: プレイヤーはマウス等でカメラを自由に回転・ズームできる（盤面自体は回転しない）。初期視点は斜め見下ろし
  - PC: 左ドラッグ＝回転、ホイール＝ズーム（右ドラッグでの位置移動＝パンは無効化）
  - スマホ: 1本指ドラッグ＝回転、2本指ピンチ＝ズーム
  - 盤面中心を注視点として固定するため、パンは無効化している（盤面を画面外に見失う事故を防ぐため）
- **カーソル移動はカメラ相対**: 上下左右の移動は、ワールド座標に固定せず「現在のカメラの向きから見て画面上でどちらが奥・手前・左・右か」を毎回計算してから盤面のグリッド軸に割り当てる。これによりカメラをどれだけ回転させても、Upは常に画面奥、Downは常に画面手前、Left/Rightは常に画面の左右に対応し、操作感が反転しない
- **色ハイライトボタン**: 設定されたカラー段階数ぶんのトグルボタンを画面上に配置。押した色以外のオブジェクトを半透明化し、選んだ色の位置を把握しやすくする（複数色を同時選択可能）
- **中断メニュー**: ゲーム画面にメニューボタン（☰）を常設し、押すと一時停止画面を表示する。一時停止画面には「ゲームに戻る」「タイトルへ戻る」の2択があり、タイトルへ戻る場合は現在のスコアをその時点でハイスコアと比較・保存してからタイトルに遷移する

## 4. 合体ルール

### 4.1 段階ごとの形状

色の段階が上がるほど、1個のオブジェクトが占有するマス数が増える（形状は段階ごとに固定・回転操作で向きだけ変えられる）。基準形状は平面（1層のみ）のパターンとして定義し、縦回転（X軸ピッチ）・横回転（Y軸ヨー）を組み合わせることで、複数のY層にまたがる立体的な向きにできる。

| 段階 | マス数 | 形状 |
|---|---|---|
| 白 | 1 | ■ |
| 黄 | 2 | ドミノ（■■） |
| 赤 | 3 | L字（かぎ型） |
| 青 | 4 | T字 |
| 緑 | 5 | 十字（回転しても形が変わらない対称形） |
| 紫 | 6 | 4連＋2連の鉤型（ヘキソミノ） |
| 橙 | 7 | H字（ヘプトミノ） |
| 黒 | 8 | 中抜きの環（3×3から中心を除いた対称形） |

カラー段階数の設定（4〜8）に応じて、先頭から使用する段階数が決まる（例: 4段階なら白〜青、8段階なら白〜黒すべて）。

### 4.2 合体判定

1. オブジェクトが着地した後、着地位置（＝そのオブジェクトの占有セル）を起点に同色の連結を6方向のFlood Fillで探索する。**この時の「個数」はマス数ではなくオブジェクト（エンティティ）の個数**で数える
2. 連結したオブジェクトの個数 N が4以上であれば合体が発生する
   - 合体すると、連結した4個以上のオブジェクトはすべて盤面から消え、**常に次の色のオブジェクトを1個だけ生成**する（Nの大小によらず1個固定）
   - N-4個分の余剰は消費されるが、スコアのボーナス計算にのみ反映される（後述）
3. 次の色のオブジェクトを置く場所は、**消えたセルとその隣接1マスの範囲内**で、次の色の形状（回転含む）が「2. 盤面仕様」の設置条件（支え必須・高低差上限）を満たして収まる位置を自動で探して決める（4つ以上連結すれば、消えるマス数は次の段階が必要とするマス数より必ず多くなるため、この範囲内でほぼ解決できる）
4. **置ける場所がどうしても見つからない場合は、合体を保留する**（消滅させず、連結した状態のまま盤面に残す）。プレイヤーは大きなオブジェクトを育てるための設置スペースを確保する必要が生まれ、これが新しい戦略性になる
5. 合体で消えたセルの上に別のオブジェクトが乗っていた場合、そのオブジェクトは剛体のまま、支えを失った分だけ沈む。エンティティ内のセルごとに支えの余地（下方向の空き量）が異なりうるため、**そのエンティティが持つ全セルの「落下可能距離」のうち最小値ぶんだけ、エンティティ全体を一括で沈める**（一部のセルだけ先に沈む・片方だけ高さがズレることはない）
6. 合体で新しく生成されたオブジェクトを起点に、再度連結判定を行う（連鎖）。連鎖が発生しなくなるまで繰り返す
7. 最上位段階（設定したカラー段階数の最後の色）のグループ（4個以上）が合体条件を満たした場合は、次の色を生成する代わりに**消滅**し、特大ボーナススコアを加算する

## 5. 出現ルール

- プレイヤーが落とせるオブジェクトの色は、以下の重み付き抽選で決定する（初期値・後で調整可能）

| 色 | 出現率 |
|---|---|
| 白 | 65% |
| 黄 | 25% |
| 赤 | 8% |
| 青 | 2% |

- **直接出現するのは先頭4色（白/黄/赤/青）まで**。カラー段階数を5以上に設定した場合、5段階目以降（緑・紫・橙・黒）は直接出現せず、常に合体でのみ生成される
- 次に落とす1個の色を、常に画面上に予告表示する（先読みは1個のみ）

## 6. スコア計算

- 基本スコア = 段階係数 × 合体個数（N）
  - 段階係数（初期値）: 白=1, 黄=2, 赤=4, 青=8, 緑=16, 紫=32, 橙=64, 黒=128（段階が上がるごとに倍）
- 余剰個数（N-4）に応じたボーナス倍率: `基本スコア × (1 + 余剰個数 × 0.5)`
- 1回の設置内で連鎖が発生した場合、連鎖回数に応じた倍率を掛ける（例: 2連鎖目×1.5、3連鎖目×2.0…）
- 最上位段階の合体（消滅）時は、上記計算に加えて固定の特大ボーナス（初期値: +5000点）を加算する
- 上記の係数・倍率・ボーナス値はすべて調整可能な定数として実装する

## 7. ゲームオーバー条件

- **いずれか1列でも高さが上限（8段）に達した時点で、即座にゲームオーバーとする**（他の列に余裕があっても関係ない）
- 合体・連鎖処理が終わった後の最終的な盤面状態で判定する（合体によって上限到達が解消された場合はゲームオーバーにならない）
- ゲームオーバー後はハイスコアと比較し、更新されていれば保存する（localStorage）

### 7.1 事前警告

- カーソルが指している列の高さが「上限マイナス1」（例: 上限8段なら7段目まで積まれている状態）のとき、そこに置くと即ゲームオーバーになる。この場合、配置予定列のハイライト表示を**点滅（不透明度の周期変化）＋サイズの周期変化（拡大縮小）＋警告色**のループ演出に切り替え、プレイヤーに危険を知らせる

## 8. 視覚仕様

- 各段階は、色に加えて**占有マス数・形状（4.1参照）**でも差別化する
- 1マスあたりの見た目のサイズ自体は全段階で統一する（グリッドに揃えて配置するため、重なり時に違和感が出ないようにする）。色ごとの図形バリエーション（白=球／黄=立方体／赤=八面体／青=円錐／緑=トーラス／紫=正十二面体／橙=正二十面体／黒=トーラスノット）で見分けやすくする
- **同一エンティティ内で隣接するセル同士は、高さが同じか違うかに関わらず、常に「紐（ボンド）」で接続表示する**。オブジェクトの見た目のサイズがマス間隔より小さく隙間ができるため、高さが揃っている場合でも紐を省略しない。デフォルメされた幻想の分子結合というテイストとし、リアルな分子モデルの再現は目指さない。これにより地形追従で物理的に離れたセルであっても「1つの塊」であることが視覚的に伝わるようにする
- 着地時は物理シミュレーションではなく、簡易なバウンドアニメーション（イージング）で見た目の演出のみ行う。複数マスにまたがるオブジェクトも1つの剛体として同時に落下・着地する

## 9. 画面構成

1. **タイトル画面**: ゲーム開始への導線
2. **ルール説明画面**: 合体条件・色の段階・スコアの仕組み等の簡単な説明
3. **設定画面**: 盤面サイズ（3〜6）・カラー段階数（4〜8）・列の高さ上限（4〜12）を調整可能。盤面サイズ・カラー段階数の変更は次のゲーム開始時から反映される
4. **ゲーム本編画面**: 盤面・スコア表示・次の色予告・色ハイライトボタン・カメラ操作・中断メニューボタン
5. **一時停止画面**: 中断メニューボタンから遷移。「ゲームに戻る」「タイトルへ戻る」の2択
6. **ゲームオーバー画面**: 最終スコア・ハイスコア表示、リトライ導線

## 10. MVPスコープ

### 含むもの
- コアゲームループ（配置→合体→連鎖→スコア→ゲームオーバー）
- カメラ自由回転、色ハイライトボタンによる視認性対策
- タイトル画面、ルール説明画面、設定画面
- ハイスコアの永続保存（localStorage）

### 含まないもの
- 音声（BGM・SE）
- レイキャストによる遮蔽オブジェクトの自動半透明化（カメラ回転で代替するため不要と判断）
- マルチプレイ、ランキング等のオンライン要素

## 11. 今後調整可能なパラメータ一覧

| パラメータ | 初期値 | 設定画面での調整範囲 |
|---|---|---|
| 盤面サイズ | 4×4 | 3〜6（次のゲーム開始時から反映） |
| 列の高さ上限 | 5段 | 4〜12 |
| 色の段階数 | 7段階 | 4〜8（次のゲーム開始時から反映） |
| 出現率（白/黄/赤/青） | 65% / 25% / 8% / 2% | 設定画面からの変更は無し（Config.js側の定数） |
| 段階係数（スコア） | 白1/黄2/赤4/青8/緑16/紫32/橙64/黒128 | 同上 |
| 余剰ボーナス倍率係数 | 0.5 | 同上 |
| 連鎖倍率 | 2連鎖×1.5、3連鎖×2.0… | 同上 |
| 最上位段階消滅時の固定ボーナス | +5000点 | 同上 |
| 段階ごとの形状 | 白1/黄2(ドミノ)/赤3(L字)/青4(T字)/緑5(十字)/紫6(鉤型)/橙7(H字)/黒8(環)。各段階1種類固定 | - |
| 隣接セルペアの高低差上限 | 1段 | - |
