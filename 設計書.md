# 設計書（3D連結マージゲーム デモ）

対応する仕様は `仕様書.md` を参照。本書は実装方針（技術構成・データ構造・アルゴリズム）を定義する。

## 1. 技術スタック

- **描画**: Three.js
- **読み込み方式**: npm・ビルドツール不使用。`<script type="importmap">` によりCDN（例: esm.sh, unpkg）からES Modulesとして直接読み込む
- **物理エンジン**: 使用しない（Cannon-es/Rapierは採用しない）。合体・接続判定はすべてグリッド座標上のロジックで行い、落下・着地の見た目はTween（線形補間＋イージング関数の自作、または軽量な自作Tweenユーティリティ）で表現する
- **状態保存**: `localStorage`（ハイスコアのみ）
- **構成**: 単一HTMLファイル＋複数JSファイル（ES Modules）。バンドラは使わない

## 2. ディレクトリ構成（案）

```
Demo_1/
├── index.html              # importmap定義、エントリポイント
├── style.css                # UI（タイトル/ルール説明/設定/HUD）のスタイル
├── src/
│   ├── main.js               # 初期化、画面遷移（ステートマシン）の起点
│   ├── scene/
│   │   ├── SceneManager.js   # Three.jsのScene/Camera/Renderer初期化、OrbitControls
│   │   └── ObjectFactory.js  # 色ごとの単一マス用メッシュ生成（複数マス形状への組み立てはGameControllerが担当）
│   ├── game/
│   │   ├── GridModel.js      # 盤面のデータ構造・状態管理（純粋ロジック、Three.js非依存）
│   │   ├── MergeResolver.js  # Flood Fillによる連結判定・合体・連鎖処理
│   │   ├── SpawnManager.js   # 次に落ちる色の重み付き抽選・予告管理
│   │   ├── ScoreManager.js   # スコア計算式
│   │   └── GameController.js # 上記を束ねる、1プレイのゲームループ制御
│   ├── ui/
│   │   ├── TitleScreen.js
│   │   ├── RuleScreen.js
│   │   ├── SettingsScreen.js
│   │   ├── HUD.js             # スコア表示、次の色予告、色ハイライトボタン、中断メニューボタン
│   │   ├── PauseScreen.js     # 一時停止画面（ゲームに戻る／タイトルへ戻る）
│   │   └── GameOverScreen.js
│   └── input/
│       └── CursorController.js # グリッド座標としてのカーソル位置管理のみ（キー入力・カメラ変換は持たない）
└── 仕様書.md / 設計書.md
```

## 3. 座標系・データ構造

盤面は `x` (0〜`boardSize-1`), `z` (0〜`boardSize-1`) の2軸で列を表し、各列は `y` (0〜`maxHeight-1`、`maxHeight`固定長) 方向のスロット配列を持つ。`boardSize`は設定画面で3〜6の範囲で変更でき、`GridModel`のコンストラクタ引数として渡すだけで盤面サイズ全体に反映される。**1つのオブジェクト（エンティティ）が複数マスにまたがる**ため、単純な「1マス=1オブジェクト」のスタックではなく、以下のエンティティモデルを採用する。

```js
// GridModel:
//   columns[x][z][y] = { entityId, color } | null   // 固定長(maxHeight)の配列。nullは空き
//   entities: Map<entityId, { id, color, cells: [{x, z, y}, ...] }>
//
// 1つのentityは、色ごとに固定された形状（Config.shapes）のぶんだけ複数の cells を持つ。
// cellsのyは、着地時の地形に応じて (x,z) 列ごとに独立して決まるため、
// 同一entity内でも一定とは限らない（4.1参照）。
```

- `getHeight(x, z)`: 列の先頭からnullを探索して返す。**settle()の直後は常にギャップが無い前提**（invariantとして維持する）
- 形状データは `Config.js` の `SHAPES` に段階ごとの基準形状（ローカル座標`(dx, dy, dz)`のセルオフセットリスト）を持つ。回転は縦回転(X軸)・横回転(Y軸)それぞれ4状態の組み合わせ（最大24通り、対称形は重複除去）を事前計算する（`GridModel`や`MergeResolver`はここを参照するだけで、形状・回転のロジックを自前で持たない）
- Three.js側は `GameController.entityMeshes: Map<entityId, THREE.Group>` でエンティティ単位のビューを管理する。1つの`Group`の中に、そのエンティティが占有するセルの数だけ`ObjectFactory.createObjectMesh(color)`の子メッシュを並べる（見た目は「同じ色・同じサイズのマスがいくつか繋がって1つの形になっている」というクラスタ表現。継ぎ目のない単一メッシュへの統合はしない）
- 子メッシュは`(dx*cellSize, dy*cellSize, dz*cellSize)`という、着地時に確定した**エンティティ内での相対ローカル座標**を持つ（配置時に1度だけ計算し、以後は固定）。`Group`の`position.y`がエンティティの基準高さを表し、これをTweenで動かせば、複数マス・複数段にまたがる形でも1つの剛体として正しく落下・着地・除去アニメーションができる（設計としては旧版と同じ「Groupのposition.yだけ動かす」方式を維持できるが、子メッシュのローカルyが常に0だった旧版と異なり、非0の値を取りうる点が変わる）
- 紐（ボンド）で繋ぐセルのペアは、**着地後のワールド座標ではなく、着地前のローカルオフセット（回転後・地形追従前の`(dx,dy,dz)`）から一度だけ計算する**（`Config.computeLocalBondIndexPairs`）。3軸のマンハッタン距離が1のペア（＝形状定義上もともと隣接するセル同士）だけを繋ぐ。ワールド座標（着地後のx,z,y）を基準に隣接判定すると、地形追従で列ごとの高さがバラバラになった際に、本来隣接しないセル同士（例: 縦積みで別のセルと同じ列に来たセルと、その隣の列にある無関係なセル）まで誤って繋がってしまうため、この判定はローカル形状のみで行う
- 計算したインデックスペア（`bondPairs`）は、着地時に`GridModel.placeCells(worldCells, color, bondPairs)`でエンティティに1度だけ保存し、以後は固定情報として扱う（`settle()`で座標が変わっても再計算しない）。`GameController._createEntityGroup`は`entity.bondPairs`を参照して、対応する2セルの中心を結ぶ**紐（ボンド）メッシュ**を同じ`Group`の子として追加する（8節・4.4節参照）。オブジェクト自体の見た目のサイズがマス間隔より小さく隙間ができるため、高さが揃っている隣接セル同士も省略せず繋ぐ。`Group`ごと動かすだけでボンドも一緒に追従する

## 4. 主要アルゴリズム

### 4.0 段階ごとの形状・回転

- `Config.SHAPES[color]` は基準形状（ローカルセルオフセット `[[dx, dy, dz], ...]`。基準姿勢では`dy`はすべて`0`＝平面形状）を持つ
- 回転は「縦回転（X軸ピッチ）」「横回転（Y軸ヨー）」の2つを、それぞれ`0〜3`（90度刻み4状態）の独立したカウンタとして扱う。`GameController.pendingPitch` / `pendingYaw` が現在のオブジェクトの向きを保持し、`SpawnManager.advance()`で次のオブジェクトに切り替わるたびに両方とも`0`へリセットする
- 回転操作は「縦回転ボタン」が`pendingPitch`を`+1 mod 4`、「横回転ボタン」が`pendingYaw`を`+1 mod 4`するだけ。現在の実オフセットは、基準形状に対して**ピッチ→ヨーの順に回転行列を適用**して毎回導出する（`Config.js`側で24通り分を事前計算しキャッシュしてもよい）
- 1マスの形状（白）は回転しても見た目・当たり判定が変わらないため、両ボタンとも無効化表示にする（`SHAPES[color].length <= 1`で判定）。緑（十字、対称形）のように、特定の軸回転で見た目が変化しない形状は、該当ボタンのみ無効化してもよい
- 回転後のオフセットは `(dx, dy, dz)` を持つ。これを **(dx,dz)をキーにグルーピング**すると、同じ`(x,z)`列内で複数セルが縦に積まれる場合（＝ピッチによって元は水平だった並びが垂直になった場合）を検出できる。この列内グループの内部相対順序（どちらが下でどちらが上か）は**地形に関係なく固定**（4.1参照）

### 4.1 落下・着地（モデルP: 列ごとに独立着地）

1. `CursorController`が保持するアンカー座標`(x, z)`と、現在の形状・`pendingPitch`/`pendingYaw`から、回転後のローカルオフセット一覧`[{dx, dy, dz}, ...]`を計算する
2. オフセットを`(dx, dz)`ごとにグルーピングし、**列グループ**（同じワールド列`(x+dx, z+dz)`を共有するセルの集まり。各グループ内の`dy`はグループ内最小値を0とした相対値に正規化する）を作る
3. 各列グループについて、対応するワールド列`(worldX, worldZ) = (x+dx, z+dz)`の現在の高さ`grid.getHeight(worldX, worldZ)`を「着地基準高さ」とする。グループ内の各セルの絶対`y`は`着地基準高さ + 正規化済みdy`となる（＝そのセル群は、通常のスタック配置と同様、列の一番上に乗るだけ。地形への追従はここで自動的に実現される）
4. 配置可否は以下を満たすかで判定する（`GridModel.canPlaceFootprint`相当）
   - すべてのワールド列が盤内であること
   - すべてのセルの絶対`y`が`config.maxHeight`未満であること
   - **形状上で隣接する、異なる列グループ同士**について、両グループが占有する絶対y範囲（`loAbs`〜`hiAbs`。縦積みの場合は底から一番上まで）の**隙間**が`config.maxAdjacentHeightDiff`（初期値1）以下であること。範囲が重なっていれば隙間は0として扱う。列グループの底同士ではなく、隣接列に一番近いセル同士（＝隙間が一番小さくなる組み合わせ）で判定するため、縦積みの上段が隣の列に近ければ、下段がそれ以上離れていても配置できる。同一列グループ内（ピッチにより縦積みになった部分）は地形追従の対象外のため、この上限チェックは行わない
5. 配置可能なら`GridModel.placeFootprint(...)`で新しいエンティティを生成し、`entityId`を得る。この時点で各セルの絶対`y`が確定し、以後エンティティは剛体として扱われる（3節参照）
6. 対応する`THREE.Group`（cells分の子メッシュ＋立体的に隣接するセル間のボンドメッシュ）を生成し、盤面上空から目標のY座標まで、Group単位でTweenアニメーション＋着地時のバウンドイージングを再生する
7. アニメーション完了後、`MergeResolver.resolveMerges(grid, entityId, config)`で合体判定を開始する

**配置プレビュー（3Dゴースト）**: カーソル移動・回転のたびに、上記1〜4の判定処理（着地高さの計算・配置可否判定）をそのまま再利用し、その結果を使って半透明のゴースト`Group`をカーソル位置に表示・更新する。判定ロジックを二重に持たず、実際の配置処理と共通化する。配置不可の場合はゴーストのマテリアルを赤系に切り替える。床面のマス目ハイライト（列単位の半透明パネル）も従来通り併用する

### 4.2 連結判定・合体（エンティティ単位のFlood Fill）

- 着地したエンティティを起点にBFS/DFSで、そのエンティティが占有する**全セル**それぞれについて6方向（`+x, -x, +z, -z, +y, -y`）の隣接セルを調べ、同色の別エンティティを発見したらグループに加えて探索を続ける（1セル単位ではなく、エンティティ単位でグラフ探索する）
- 連結した**オブジェクトの個数**（マス数ではない）`N >= mergeThreshold(4)` の場合:
  1. 次の色が最終段階でなければ、消えるグループの全セル＋その隣接1マスの範囲だけを探索範囲にして、次の色の形状（縦回転・横回転の組み合わせ、最大24通りすべて試す）が4.1節の配置条件（列ごとの着地・隣接列の高低差上限）を満たす位置を探す（`findPlacementNearVacated`）。**盤面全体は探索しない**（4個以上の連結で必ず消えるマス数の方が次段階の必要マス数より多くなるため、この範囲で大半は解決できるという前提）。見つかった場合は`{ worldCells, bondPairs }`を返す（`bondPairs`はヒットした向きのローカル形状から`computeLocalBondIndexPairs`で計算し、`grid.placeCells()`にそのまま渡す）
  2. 置ける場所が見つからなければ、**この合体自体を保留する**（グループは除去せず、そのまま盤面に残す）。ただしチェーン全体は打ち切らず、`heldKeys`にそのグループの識別キー（含まれるentityIdの集合）を記録して**他のグループの走査は続ける**（保留グループを再検出して無限ループに陥らないため）
  3. 置ける場所が見つかった場合のみ、グループの全エンティティを`removeEntities()`で除去し、`settle()`で重力を適用し、その後`placeCells()`で次の色のオブジェクトを生成する。合体が1回でも成立すると盤面が変化するため`heldKeys`はクリアし、保留していたグループも再評価する
  4. **毎ラウンド盤面全体を再スキャン**して、合体条件を満たす連結グループが無くなるまで繰り返す（`findMergeableGroup`）。合体後の`settle()`で新たに繋がった別の同色グループも取りこぼさず連鎖させるため（例: 白-黄-白の層構造で中間の黄が消え、上下の白同士が繋がって合体するケース）。直前に置いた／生成したオブジェクトの周辺グループは`preferredSeedId`で優先的に処理し、演出順を自然に保つ。連鎖カウントは合体成立ごとに加算しスコア計算に渡す
- 色が最上位段階（設定したカラー段階数の最後の色）でグループが成立した場合は次の色を生成せず、消滅処理のみ行い（置き場所探索も不要）、特大ボーナスをスコアに加算する
- **既知の簡略化**: 置き場所探索は除去前の盤面状態をもとにした簡易シミュレーションで行っており、複数マスにまたがる別のエンティティが除去対象の真上に一部だけ乗っている稀なケースでは、実際の`settle()`後の結果と食い違う可能性がある。発生頻度・影響とも小さいため、この版では対応しない

### 4.3 重力（settle）

- `GridModel.settle()`は、**列（x,z）ごとに独立して落とす**方式を採用する:
  1. エンティティのセルを`(x,z)`列でグルーピングする。同じ列内で縦積みになっているセル同士は一体で落とすが、同一エンティティでも異なる列は互いに独立して落とす
  2. 各列グループについて、落下量計測の間だけ自セルを盤面から一時的に取り除き（同じ列の縦積みセルを支えと誤認しないため）、その列で真下に連続して空いているスロット数ぶん落とす
  3. どのエンティティも動かせなくなるまで繰り返す（連鎖落下にも対応）
- **なぜ「剛体一括落下」ではなくこの方式か（重要・再検討時の判断基準）**: このゲームの配置・高さ・ゲームオーバー・危険警告はすべて`getHeight(x,z)`＝「列の下から最初のnullまでの高さ、settle後はギャップ無しが前提」に依存している。剛体として全セルを最小落下量だけ動かす方式だと、「片側が支えられ、もう片側が穴の上に出っ張る」エンティティが**穴の上のセルを下に空きスロットを持ったまま浮かせる**（＝列にギャップができる）。すると`getHeight`が浮きセルより下の値を返し、`computeLanding`が浮きセルの下に新オブジェクトを重ねて配置する／`isGameOver`・危険警告が浮きセルを数え損なう、という不具合の温床になる。列ごと独立落下なら全セルが必ず何かの上に着地し、このギャップ無し不変条件を常に保てる
- **トレードオフ**: 複数列にまたがるエンティティは、支えの差で列ごとに異なる高さに落ち、形が縦に伸びうる（紐が長くなる）。ただし確定した盤面設定（4×4・高さ5）では発生が限定的で、色ハイライト機能もあるため実用上許容する
- 呼び出し側（`GameController._animateSettle`）は、`settle()`が返す各エンティティの情報を使う。全セルが同じ量だけ沈んだ場合（`uniform: true`）は`Group.position.y`を一括で動かすだけ。列によって沈下量が異なる場合（`uniform: false`）は、Groupを作り直さず（再生成に見えないため）、`before`/`after`スナップショットから各子メッシュのローカルYと`Group.position.y`を同時補間し、`entity.bondPairs`に対応するボンドメッシュ位置を毎フレーム追従させる

### 4.4 カーソル移動のカメラ相対変換

カメラが自由回転できる（後述）ため、矢印キー／十字ボタンの上下左右をワールド座標に固定すると、カメラを回転させた際に画面上の見た目と操作方向が逆転してしまう問題があった。これを避けるため、キー入力そのものは `GameController` が受け取り、押されるたびに現在のカメラの向きから画面上の上下左右をワールド座標のグリッド軸へ変換してから `CursorController.move(dx, dz)` を呼び出す。

1. `camera.getWorldDirection()` でカメラの視線方向を取得し、Y成分を0にして正規化 → `forward`（画面奥方向）
2. `right = cross(forward, worldUp)` で画面右方向を算出
3. `forward` / `right` それぞれをX軸・Z軸のうち絶対値が大きい方に丸め、`(±1, 0)` または `(0, ±1)` にスナップする（盤面はグリッドなので、斜め移動は発生させない）
4. Up→`forward`、Down→`-forward`、Right→`right`、Left→`-right` として `dx, dz` を決定する

これにより、カメラをどの角度に回転させても「Up＝画面奥」「Down＝画面手前」「Left/Right＝画面の左右」が常に一致する。`CursorController` 自体はカメラや入力方式を一切知らず、グリッド座標の保持と移動のクランプ処理のみを担当する。

## 5. カメラ制御

- Three.jsの `OrbitControls` を使用し、盤面中心を注視点として設定
- 初期カメラ位置は斜め見下ろし（例: 盤面中心から仰角45度程度、距離は盤面全体が収まる値）
- ズーム・回転はプレイヤーの入力（マウスドラッグ／ホイール、タッチ）にそのまま委譲する。盤面オブジェクト自体は回転させない
- `controls.enablePan = false` によりパン（位置移動）は無効化する。盤面中心が常に注視点として固定されるため、PCの右ドラッグは無効化、スマホの2本指操作はズームのみとなり、盤面を画面外に見失う事故を防ぐ
- カメラの向きはカーソル移動の方向解決（4.4）にも利用される

## 6. 色ハイライトボタン

- `config.colors`（カラー段階数ぶんの配列）をもとに、`HUD.rebuildHighlightButtons(colors)` がボタンDOMを動的に生成する（`index.html`側は空のコンテナ`#hud-highlight-buttons`のみ用意し、固定の`<button>`は持たない）。カラー段階数の設定変更が反映される次のゲーム開始時に、`main.js`の`startNewGame()`から呼び出す
- トグル状態（`Set<string>`）をUI側で保持
- 選択中の色が1つ以上ある場合、選択されていない色のエンティティ（`Group`内の全子メッシュ）の `opacity` を下げる（例: 0.15）。選択が空の場合は全オブジェクトを通常表示に戻す
- レイキャストによる遮蔽物検出は実装しない（カメラ自由回転で代替するため）

## 7. スコア・状態管理

- `ScoreManager` は仕様書のスコア計算式をそのまま実装し、合体・連鎖のたびに加算値を返す
- ゲームオーバー時、`localStorage` の既存ハイスコアと比較し、上回っていれば更新する
  - キー例: `demo1_highscore`
- `GridModel.isGameOver()` は「いずれか1列でも `canPlace()` が `false`（上限到達）」を検出したら `true` を返す。全列が埋まるまで待つ実装にしないこと（意図的にこの1列判定を採用している）
- `GameController._updateCursorVisual` は、現在の形状が占有する全セルの高さが揃っていて（＝配置可能）、かつその高さが `maxHeight - 1` のとき `cursorDangerous = true` にする。`GameController.update(now)` が毎フレーム呼ばれ、`cursorDangerous` の間はカーソルのハイライトメッシュ（形状のセル数ぶん全部）を `Math.sin(now * ...)` ベースで不透明度・スケール・色を周期的に変化させ、点滅＋サイズ変化のループ演出にする（Tweenキューを使った一過性アニメーションとは別に、常時ループする描画更新として `main.js` の `animate()` から呼び出す）

## 8. 画面遷移（ステートマシン）

```
Title → RuleExplanation → (Settings は Title からもアクセス可)
Title → Game → GameOver → Title（またはGameへリトライ）
Game ⇄ Pause → Title（中断メニューからの離脱。スコアはハイスコアと比較・保存してから遷移）
```

- 中断メニュー: HUDのメニューボタン（☰）押下で `GameController.pause()` を呼び、入力を無効化して `PauseScreen` を表示する。「ゲームに戻る」で `GameController.resume()` を呼び `HUD` に戻る。「タイトルへ戻る」では、ゲームオーバー未確定でもその時点のスコアをハイスコアと比較・保存してからタイトルへ遷移する
- `pause()`/`resume()` はアニメーション中（`busy`）の状態と独立したフラグ（`paused`）で管理し、演出の途中で中断してもアニメーション完了時に誤って操作を再有効化しないようにする

- `main.js` が現在の画面状態を保持し、各Screenクラスの `show()` / `hide()` を切り替える単純なステートマシンとする

## 9. 調整可能パラメータの置き場所

- `仕様書.md` の「今後調整可能なパラメータ一覧」に対応する定数を、`src/game/` 内に `Config.js` として一箇所にまとめ、マジックナンバーを散らさない
- `Config.js`の`COLORS`は8色ぶんのマスター順序（白〜黒）を持ち、`colorLevelCount`（4〜8）に応じて`COLORS.slice(0, colorLevelCount)`で先頭から使用する色を切り出す。`baseShapes`/`tierCoefficient`/`spawnWeights`もこの`colors`配列から`deriveColorLevelFields()`で再計算する（`spawnWeights`は先頭4色まで固定の重み、それ以外は常に0＝合体専用）
- 設定画面（`SettingsScreen`）は`config.boardSize`・`config.colorLevelCount`・`config.maxHeight`を直接書き換えるだけで、派生データの再計算は行わない。`main.js`の`startNewGame()`が呼び出す`applyDerivedConfig(config)`が、その時点の`boardSize`/`colorLevelCount`をもとに`colors`/`baseShapes`/`tierCoefficient`/`spawnWeights`を同一オブジェクト上で更新してから`GameController`を生成する
- `boardSize`の変更は`SceneManager`（床の広さ・カメラ位置）にも影響するため、`startNewGame()`のたびに既存の`SceneManager`を`dispose()`（リサイズリスナー除去・WebGLコンテキスト解放）してから作り直す。ゲームの再挑戦（「もう一度」）のたびに毎回作り直す方式とし、「設定変更後だけ作り直す」ような条件分岐は持たない（実装をシンプルに保つため）
